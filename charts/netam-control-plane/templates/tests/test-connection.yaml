apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netam-control-plane.fullname" . }}-test-connection"
  labels:
    {{- include "netam-control-plane.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netam-control-plane.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
