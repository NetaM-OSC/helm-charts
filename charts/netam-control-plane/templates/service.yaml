apiVersion: v1
kind: Service
metadata:
  name: {{ include "netam-control-plane.fullname" . }}
  labels:
    {{- include "netam-control-plane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netam-control-plane.selectorLabels" . | nindent 4 }}
